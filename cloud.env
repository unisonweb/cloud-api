#!/bin/bash

export CLOUD_DEPLOYMENT=local
export SHARE_API_ORIGIN=https://api.unison-lang.org
export CLOUD_API_ORIGIN=http://localhost:5424
export NOMAD_ALLOC_HOST_cloud_http=localhost
export NOMAD_HOST_IP_cloud_tcp=0.0.0.0
export NOMAD_ALLOC_PORT_cloud_http=5424
export NOMAD_ALLOC_PORT_cloud_tcp=5425
export CLOUD_CLIENT_HOSTNAME=0.0.0.0
export CLOUD_CLIENT_PORT=8500
export CONSUL_HOST=http://127.0.0.1:8500
export CLOUD_AWS_CREDENTIAL_URL="invalid"
# Chris's test app
export CLOUD_REDIS=redis://localhost:6379
export CLOUD_POSTGRES=postgresql://postgres:sekrit@localhost:5432
export CLOUD_POSTGRES_CONN_TTL=30
export CLOUD_POSTGRES_CONN_MAX=10
export CLOUD_CERT=./testing-certs/enlil.cert
export CLOUD_PRIV_KEY=./testing-certs/enlil.key
export CLOUD_REDIS_CERT=./testing-certs/redis.cert
export CLOUD_HMAC_KEY="gpeakbroleymbscyqzrcalpemrjayhur"
export CLOUD_EDDSA_KEY="cloud-eddsa-key-cloud-eddsa-key-"
export SHARE_EDDSA_KEY="share-eddsa-key-share-eddsa-key-"
export CLOUD_SHARE_UI_ORIGIN="http://localhost:1234"
export CLOUD_CLOUD_UI_ORIGIN="http://localhost:5678"
export CLOUD_CLOUD_HOMEPAGE_ORIGIN="http://localhost:5679"
export CLOUD_LOG_LEVEL="DEBUG"
export CLOUD_COMMIT="dev"
export CLOUD_API_SUFFIX="byoc.unison.cloud"

# Placeholder, which are overwritten by secrets.env; contact Chris if you need
# valid secrets for these.
export CLOUD_ZENDESK_API_USER="invaliduser@example.com"
export CLOUD_ZENDESK_API_TOKEN="bad-password"
# Change this TO A *TEST* API key (should start with sk_test or rk_test) to test Stripe integration locally
# The easiest way is to:
# - install the Stripe cli
# - run `stripe login`
# - copy the test_mode_api_key from ~/.config/stripe/config.toml
export CLOUD_STRIPE_API_SECRET_KEY="rk_test_abcd1234abcd1234abcd1234"
# Change this to a test webhook secret key to test Stripe integration locally
# The easiest way is to:
# - install the Stripe cli
# - run `stripe login`
# - run `stripe listen --forward-to localhost:5425/stripe/events`
# - copy the signing secret that is printed to the screen
export CLOUD_STRIPE_WEBHOOK_SECRET_KEY="whsec_abcd1234abcd1234abcd1234"
export CLOUD_SHARE_OAUTH_CLIENT_ID="invalid"
export CLOUD_SHARE_OAUTH_CLIENT_SECRET="invalid"
export CLOUD_CLOUD_SERVICE_BUCKET="unison-cloud-services"
export CLOUD_ENVIRONMENTS_MOUNT="secret"
export VAULT_HOST="http://127.0.0.1:8200/v1"
export VAULT_TOKEN="vault-plaintext-root-token"
export AWS_CONFIG_FILE="/home/stew/devel/cloud-api/aws-config"
export AWS_ENDPOINT_URL=https://localhost:9000
export AWS_ACCESS_KEY_ID="unison-test"
export AWS_SECRET_ACCESS_KEY="sekret-for-tests"
export AWS_DEFAULT_REGION="us-west-2"
export AWS_REGION="us-west-2"
